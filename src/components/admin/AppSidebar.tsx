/**
 * @deprecated Legacy sidebar — navigation now lives in AdminShell + `@/lib/nav/admin-nav`.
 * Kept as a no-op so old imports don't break the build.
 */
export function AppSidebar(_props: { pathname?: string; permissions?: string[] }) {
  return null;
}
