---
import AdminLayout from '../../../layouts/AdminLayout.astro';
import ProductForm from '../../../components/admin/ProductForm';
import { getDb } from '../../../server/db';
import { listAllBrands } from '../../../server/repos/admin';

const brands = (await listAllBrands(getDb())).map((b) => ({ id: b.id, name: b.name }));
---

<AdminLayout
  title="Yeni ürün"
  breadcrumbs={[
    { href: '/admin', label: 'Admin' },
    { href: '/admin/products', label: 'Ürünler' },
    { label: 'Yeni' },
  ]}
>
  <h1 class="mb-6 font-display text-2xl font-bold">Yeni ürün</h1>
  <ProductForm client:load mode="create" brands={brands} />
</AdminLayout>
