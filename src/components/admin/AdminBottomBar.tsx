/**
 * @deprecated Mobile nav is handled by the kit Sidebar Sheet.
 * Kept as a no-op for AdminChrome / bulk-edit compatibility.
 */
export function AdminBottomBar(_props: { pathname?: string; permissions?: string[] }) {
  return null;
}
